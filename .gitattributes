# Unix scripts must keep LF endings or they break on macOS/Linux
# (a CR in the shebang line makes the script fail to run).
*.sh        text eol=lf
*.command   text eol=lf

# Windows scripts keep CRLF
*.ps1       text eol=crlf
*.cmd       text eol=crlf
